emacs
=> sudo apt install emacs-nox
(copy .emacs config file to ~/)
(copy personalized theme file to ~/.emacs.d/)

pycodestyle
=> sudo apt install python3-pip
=> pip install pycodestyle

Tkinter
=> sudo apt-get install python3-tk
=> python3 -m tkinter (to verify installation)

Adding directory to PATH
=> open the .bashrc file
=> paste the export syntax at the end of the file
   export PATH="/Directory:$PATH"
=> save and exit
=> execute the script or reboot the system

git configuration
=> git config --global user.email "user@email.com"
=> git config --global user.name "first_name last_name"
=> git config --global user.username "username"
=> git config --global core.editor "file_editor"
=> git config --global init.defaultBranch "main"

mdl (Markdown linter)
=> sudo apt install ruby-rubygems
=> sudo gem install mdl

mdless (Markdown viewer)
=> sudo gem install mdless

glow (Markdown viewer)
=> sudo mkdir -p /etc/apt/keyrings
=> curl -fsSL https://repo.charm.sh/apt/gpg.key | sudo gpg --dearmor -o /etc/apt/keyrings/charm.gpg
=> echo "deb [signed-by=/etc/apt/keyrings/charm.gpg] https://repo.charm.sh/apt/ * *" | sudo tee /etc/apt/sources.list.d/charm.list
=> sudo apt update && sudo apt install glow
